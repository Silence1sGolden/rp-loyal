import { CustomContainer } from '@/shared/ui/CustomContainer/CustomContainer';
import { CustomLabel } from '@/shared/ui/CustomLabel/CustomLabel';

export function ProfileStories() {
  return (
    <CustomContainer>
      <CustomLabel label="Active stories" />
    </CustomContainer>
  );
}
